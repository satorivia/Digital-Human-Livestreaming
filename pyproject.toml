[tool.pytest.ini_options]
pythonpath = ["apps/api-server", "packages/event-schema", "packages/shared-types"]
testpaths = ["apps/api-server/tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
